# ── 依赖 ─────────────────────────────────────────────
node_modules/
.pnpm-store/

# ── 构建产物（Cloudflare Pages 会在云端重新构建） ──────
dist/
dist-ssr/
build/
*.local

# Vite 临时生成文件
vite.config.ts.timestamp-*.mjs

# ── 日志 ─────────────────────────────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── 环境变量 / 密钥（严禁提交） ───────────────────────
.env
.env.*
!.env.example

# ── 编辑器 / 系统文件 ────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# ── 本地验证截图（调试产物，非游戏资源） ───────────────
verify-*.png

# ── 原始素材源文件（仅构建源，不参与部署；部署只需 public/ 下已转好的 webp） ──
background picture/
npc picture/
chaju picture/
