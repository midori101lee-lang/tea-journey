# 2026-09-13

## GitHub Push 内容清点（仅整理，未 commit/push）
- 分支 `main` 与 `origin/main` 同步（无 ahead commit）；待 push 内容全部是未提交工作树改动：32 个 tracked 修改 + 约 30 个 untracked 新文件，约 +1178 行。
- 备份：`/tmp/tea-game-backup-20260912-2217.tar.gz`（247M）完好，未被动过。
- **关键合规校验（用户硬要求）**：源里 `SHARE_PAYOUT_ENABLED=false`（gameStore.ts:21），`grantShareReward` 不改 coins、仅落当日日期；`navigator.clipboard/execCommand` 仅出现在注释、无真实调用 → Web/GitHub 均不含"分享得茶钱"逻辑。✅
- XHS 构建产物隔离：`dist/` 与 `dist-xhs/` 均 gitignored；`tea-journey-xhs.zip`（7.9M 部署包）与 `封面图.png` 为 untracked，不应入库。
- **应 Push**：全部 `src/`（含 XHS 源码 xhs.html/vite.config isXhs 分支/platform.ts/ShareSheet.tsx 等，均为源码非产物）+ `public/assets/*` 新美术 + `index.html`(仅标题) + `.gitignore`(加 dist-xhs/) + 4 份 XHS 文档。
- **不应 Push / 待确认**：`tea-journey-xhs.zip`、`封面图.png`、`.skill/`（下载的 XHS 合规 skill 工具包 84K）、`.workbuddy/memory/*`（项目内部日志，已 tracked）。
- 待用户决定是否 commit/push；本次只做清点与检查，未改任何代码、未删文件。

## ✅ Push 已完成（用户授权）
- commit `92a41fa`（53 files changed, +3265 / -151），`git push origin main` 成功：`3f92048..92a41fa main -> main`。
- 实质纳入：全部 `src/`（含 XHS 源码）+ `public/assets/*` 新美术（杭州玻璃杯/绿豆糕/牛姐/3 张场景）+ `index.html`/`vite.config.ts`/`xhs.html`/`.gitignore` + 4 份 docs + `.workbuddy/memory/*`。
- 严格排除 XHS 部署产物：`dist-xhs/`（gitignored）、`tea-journey-xhs.zip`、`封面图.png`、`.skill/`（均保持 untracked，未入库）。
- 注意文件名校正：实际文件是 `public/assets/teaware/杭州玻璃杯.webp` 与 `docs/XHS_分享机制_阶段开发报告.md`（早前清点误记为 玻璃杯.webp / 部署产物_校验报告.md）。
- CF Pages 应已触发自动重建；"已更新"需 CF 部署成功实测确认（本环境无法验证部署结果）。SHARE_PAYOUT_ENABLED 仍 false。

## 茶集市卖茶 + NPC 对话两项优化（已实现，未 commit/push）
- 关键架构发现：`XhsApp.tsx:37` 直接 `return <WebApp />`，Web/GitHub/XHS 实为同一代码路径，故改共享组件即三端同步，无需任何 `IS_XHS` 分支。
- **茶集市卖茶（灵活出售）**：`gameStore.sell(stackId, price, count?)` 增加可选 `count`（缺省整包，向后兼容）；`MarketView` 新增 `sellQty` 选择态 + 每包勾选框 + 数量步进器（默认卖1、可加到整包），"开门迎客"只处理已勾选；未选留篓；结果文案带 `×qty`；行情/定价/顾客意愿/熟人逻辑全不动。
- **NPC 对话（你一句我一句）**：`NpcDialog` 检测 `speaker==='你'` → 右对齐"我"气泡（暖绿），否则左对齐 NPC 气泡；当前台词 `key={i}` + CSS `@keyframes dlg-in`（淡入+上滑，0.28s）逐句浮现；仍逐句推进、不改文案/分支/特效。`base.css` 加 `.dialog-bubble*` 与 `.trade-pick/.sell-qty/.sell-summary` 样式（共享 Web+XHS）。
- 验证：`tsc --noEmit` 通过；`build:web` 与 `build:xhs` 均成功（dist/、dist-xhs/ 均 gitignored，未污染仓库）。SHARE_PAYOUT_ENABLED 仍 false。
- 用户拍板的交互细节：动画=淡入+上滑；范围=仅主线 NpcDialog（偶遇 EncounterLayer / 茶席周伯暂未改）；卖茶默认=勾选卖1、步进器加到整包。
- 未 commit/push（等用户允许）。改动文件：gameStore.ts、MarketView.tsx、NpcDialog.tsx、base.css。

## 下一版优化 ③④⑤⑥（已实现，未 commit/push）
- 三端同步机制：`XhsApp.tsx` 直接 `return <WebApp />`，改动全在共享组件/CSS/数据，零 `IS_XHS` 分支；`tsc --noEmit` + `build:web`(dist/) + `build:xhs`(dist-xhs/) 全绿。SHARE_PAYOUT_ENABLED 仍 false。
- **③ 今日行情位置**：`base.css` 新增 `.market-quote` 卡片（顶部强调条 + 阴影 + 下边距 16px 拉开层级/留白），`MarketView.tsx:191` 的「今日行情」模块套用；功能/文案/信息不变，纯视觉分层，不再与小满对话粘连。
- **④ 山路/梅家坞"我的回应"**：复用任务②的 `.dialog-bubble` 体系，对偶遇层 `EncounterLayer.tsx`（武夷山山路）与 `StrollView.tsx`（梅家坞）做气泡化 + 按 `speaker` 分色（你=绿右对齐 / NPC=左 / 旁白=弱化斜体），`key={i}` 触发 `dlg-in` 逐句浮现。数据补玩家台词：武夷山山路 5 事件（tricycle_chat_mountain/youngfarmer_mountain/dajie_mountain/uncle_mountain/traveler_mountain）+ 梅家坞 3 NPC（linggu/lingyi/gushu）+ linggu_hangzhou_chat 末尾 + strolls 排序大姐事件。梅家坞剧情显著增厚（原单薄）。
- **⑤ NPC 赠茶概率统一（关键修正）**：原错把梅家坞赠茶事件从 1 个加到 4 个 → 单次散步赠茶率 ~24-26%，而武夷山山路免费赠茶率实测 ≈13.2%（tricycle_tea/caicha_tea/youngfarmer_tea/dajie_offer/mystery_gift 经 NPC→事件加权）。这违反"不因杭州新增茶叶单独改概率"。已修正：梅家坞仅保留 2 个赠茶事件（sorting_leaves→九曲红梅、linggu→龙井，各 weight 8）→ 赠茶率 ~12-13% 与武夷山对齐；lingyi/gushu 改为纯对话（并改其台词，不再承诺给茶），保留丰富剧情。茶种严格限定杭州（longjing/jiuquhongmei），无武夷山茶泄漏。
- **⑥ 删杭州集市专属描述**：`regions.ts:39` 杭州 market blurb 删去「（各地共用）」，与武夷山 `regions.ts:22` 统一为「买卖茶与茶具的地方。」；集市内部杭州茶叶/行情未动。
- 改动文件（累计 items1-6）：gameStore.ts、MarketView.tsx、NpcDialog.tsx、base.css、EncounterLayer.tsx、StrollView.tsx、strolls.ts、encounters.ts、dialogues.ts、regions.ts。基线 commit `92a41fa`，全部未 commit/push。
- ⚠️ 待拍板：梅家坞赠茶率是否就定在 ~13%（=武夷山）还是更慷慨；当前按"与武夷山同步"定。

## 下一版优化 ⑦⑧（茶区彩蛋隔离 + 小满反应，已实现，未 commit/push）
- **⑦ 茶区专属彩蛋严格隔离（数据/触发条件层面，非 UI 隐藏）**：
  - 偶遇引擎 `encounterEngine.ts` 按 `n.regions` 过滤 NPC。原 `laojia`（景区王霸茶 wangba 唯一来源）**无 regions 字段** → 会在杭州触发 wangba，属串台。已加 `regions: ['wuyishan']`（与 `niujie` regions:['hangzhou'] 对称）。
  - `niujie`（乌牛早 wuniuzao 唯一来源）本就 `regions:['hangzhou']` → wuniuzao 仅杭州。✅
  - 集市商品池 `marketStalls.ts` 的 `OWNER_PROFILE` 只含 rougui/shuixian/dahongpao → 两彩蛋从不进摊位（共享 MarketView 不漏蛋）。✅
  - `teas.ts` wangba.regionId='wuyishan'/wuniuzao.regionId='hangzhou' 为元数据；`TEA_SEAT_GIFT_EXCLUDED=['wuniuzao','wangba']` + `regionTeaIds` 已把彩蛋挡在茶席回礼外。✅
  - 验证：wangba 仅在武夷山可购/可得；wuniuzao 仅在杭州；两蛋不因共享集市组件串台；Web/GitHub/XHS 同一代码路径（无 IS_XHS 分支）。
  - 边界观察（待拍板）：`laojia`/`dashu`（武夷山茶商）仍作为"逛摊"摊主出现在杭州集市卖武夷山茶（generateStalls 忽略 regions，是既有"各地共用集市"设计，非彩蛋）。若要集市商品池也按茶区纯隔离，需新增杭州摊主——属更大的内容扩展，本次未动。
- **⑧ 小满对景区王霸茶的"怀疑→建议周伯"**：
  - 复用对话系统：`dialogues.ts` 新增派生条件 `xiaoman_wangba_suspect = bought_wangba && !wangba_xiaoman_done && currentRegion==='wuyishan'`，及 conditional 对话 `xiaoman_wangba_suspect`（小满起疑、不妄下定论、建议后头茶桌找周伯品鉴；落 wangba_xiaoman_done 只提醒一次）。
  - `MarketView.tsx` 加 useEffect：偶遇（老贾卖 wangba）结束回到市场时，`setTalked(false)` 重挂小满对话 → 当场触发怀疑剧情（同次逛集市即可见）。
  - 周伯品鉴**优先复用既有机制**：泡 wangba 路由到武夷山茶桌，`ZHOUBO_TASTING['wangba']` 已由茶桌话题系统触发（"你买的时候没多想吧 / 别问价，问了我替你心疼"），未另建系统；语气符合武夷山茶文化设定。
- 验证：`tsc --noEmit` + `build:web`(dist/) + `build:xhs`(dist-xhs/) 全绿；SHARE_PAYOUT_ENABLED 仍 false；本次触及文件 encounters.ts / dialogues.ts / MarketView.tsx，无 IS_XHS 分支。基线 commit `92a41fa`，全部未 commit/push（含前序 ①②③④⑤⑥）。

## 茶集市按茶区隔离商品池 + 跨茶区茶商（已实现，未 commit/push）
- 评估结论：现有架构可数据驱动最小改动实现，不需新系统。`generateStalls(day)` 忽略茶区是"杭州集市像武夷山"根因。
- **marketStalls.ts 改造**：`generateStalls(day, region)` 加 region 参数。
  - `STALL_OWNERS_BY_REGION`：wuyishan=[caicha_ayi, young_farmer, maicha_dashu, tricycle_farmer, laojia]；hangzhou=[linggu, lingyi, gushu, laojia]（后三者复用现有杭州 NPC 立绘/身份，不新增美术）。`STALL_OWNERS` 保留=wuyishan 列表兼容导出。
  - 本地茶池 `LOCAL_TEAS`：wuyishan=[rougui,shuixian,dahongpao]；hangzhou=[longjing,jiuquhongmei]。集市主体=当前茶区本地茶。
  - `CROSS_REGION_SELLERS={laojia, linggu, young_male_traveler}`：跨区茶商有 ~40% 概率额外带 1 款外地普通茶（来自另一茶区本地茶），附 `FOREIGN_NOTE` 轻量文案（"这是从武夷山带来的茶，不是杭州本地出的。"等）；`EGG_TEAS={wangba,wuniuzao}` 过滤 → 彩蛋永不进商品池。
  - 新增杭州茶数据：`TEA_DESC`/`TEA_LOOK` 补 longjing/jiuquhongmei；`SELLER_STYLE` 补 linggu(tea_person)/lingyi(honest)/gushu(casual)；`WARE_PROFILE` 补三杭州摊主。`askSeller` 加 `region?` 参数 + `WHERE_BY_REGION`（"哪里"按茶区答，杭州摊主不再说"武夷山的水仙"这类错位）。
  - `StallTea.note?` 新字段；MarketView 在摊位卡片与"看看"视图渲染该提示。
- **encounters.ts 改造（NPC 身份=商品池规则）**：
  - `laojia` 去掉 `regions`（改跨区漫游：偶遇层 + 集市摊位均可出现）；其 `laojia_wangba` 事件 `requires` 加 `currentRegion==='wuyishan'` → 王霸茶仍严格仅武夷山（老贾在杭州只卖武夷山普通茶，绝不带王霸茶出山）。
  - 7 个武夷山本地 NPC（tricycle_farmer/caicha_ayi/young_farmer/tea_dajie/roadside_uncle/maicha_dashu/mystery_tea_person）加 `regions:['wuyishan']` → 满足"其他偶遇 NPC 只售本地茶"，不串台到杭州。`niujie` 保持 `regions:['hangzhou']`（wuniuzao 仍仅杭州）。`linggu`/`young_male_traveler` 无 regions（漫游）。
  - `laojia_chat_teahouse` 加 `linesByRegion.hangzhou`（杭州茶馆指玲姨，不提武夷山老陈）。
- **验证点对照**：①杭州集市本地茶（龙井/九曲红梅）明显占多数，laojia/linggu 偶带武夷山茶并附说明；②老贾/林姑娘可带外地茶（年轻男旅客=偶遇层漫游，本不摆摊，未改其对话）；③其他偶遇 NPC 已锁本茶区，不随意售外地茶；④武夷山对称（本地茶主体，laojia 偶带杭州茶）；⑤普通茶可跨区流通；⑥彩蛋严格隔离（wangba 仅武夷山事件触发、wuniuzao 仅杭州、两蛋均不进摊位）；⑦三端同一代码路径（无 IS_XHS 分支）、`tsc`+`build:web`+`build:xhs` 全绿；⑧未新增系统/美术，仅数据+触发条件。SHARE_PAYOUT_ENABLED 仍 false。
- 边界说明：杭州随机偶遇阵容收窄为 [niujie, linggu, laojia, traveler]（本地 Wuyishan NPC 已锁回武夷山）——若觉得杭州偶遇偏稀，可后续把玲姨/郭叔也接入偶遇层，但本次未扩。
- 改动文件：marketStalls.ts、encounters.ts、MarketView.tsx、base.css。基线 `92a41fa`，全部未 commit/push（含前序 items 1-8）。

## 下一版优化 ⑨⑩⑪（已实现，未 commit/push）
- 三端验证全绿：`tsc --noEmit` + `build:web`(dist/) + `build:xhs`(dist-xhs/)；零新增 IS_XHS 分支；SHARE_PAYOUT_ENABLED 仍 false。
- **⑨ 梅家坞赠茶提示**：`StrollView.tsx` 复用 EncounterLayer 的 `.tea-gain` 内联横幅（🍵 +1 茶名（品质）+ "已放入茶篓"），toast 改为显式命名（茶名+品质+来源），解决"赠茶了但玩家没注意"。杭州赠茶内容本就正确（sorting_leaves→九曲红梅、linggu→龙井，均为杭州茶）。
- **⑩ 偶遇 NPC 跨区复用**：`strolls.ts` 新增 `hz_stroll_npc_traveler`（年轻男旅客，含玩家回应、杭州风味台词）示范跨区复用；老贾/牛姐确认不进 strolls/山路偶遇池（老贾仅 market/teahouse 场景，牛姐仅 hangzhou+market）。
- **⑪ 杭州茶上品难度提升**（全局阈值 fine≥80 未动，武夷山 zuoqing/roasting 完全未动）：
  - `teas.ts`：龙井 fixation safeBand widthBase 0.2→0.14（casual 0.26→0.18）；九曲红梅 fermentation fermentTarget [67,79]→[70,77]（casual [62,82]→[64,80]）、withering [47,59]→[49,57]（casual [43,62]→[45,60]）、rolling idealRollForce [0.46,0.68]→[0.50,0.64]（casual [0.42,0.72]→[0.46,0.68]）。
  - 步骤组件：FixationStep low 档权重 0.5→0.35；ShapingStep GOOD_AT 0.52→0.58、ok 档 credit 0.7→0.55、rushed 0.35→0.25；DryingStep 绿区 width 0.15→0.10。均为杭州专用步骤，不影响武夷山。
- 改动文件：StrollView.tsx、strolls.ts、teas.ts、FixationStep.tsx、ShapingStep.tsx、DryingStep.tsx。基线 `92a41fa`，未 commit/push。

## 老贾跨区售茶定稿规则（方案一核心+方案二预告，已实现，未 commit/push）
- 用户定稿：**本地茶=集市主体；已解锁外地茶=少量流通；未解锁外地茶=不实际出售，仅探索预告**。理由：避免未去杭州就能在武夷山买到龙井，探索顺序/图鉴认知混乱。
- **teaRegions.ts**：新增 `isTeaRegionUnlocked(id, flags)`（有 unlockFlag 由 flags 决定，否则静态 unlocked；与 TeaWorldView 卡片同规则）；TeaWorldView 改用该助手（单一判定来源）。
- **marketStalls.ts**：`generateStalls(day, region, flags?)` 加第三参；跨区茶商掷中外地茶后先判 `isTeaRegionUnlocked(对方茶区, flags)`——已解锁→照常摆出（附 FOREIGN_NOTE）；未解锁→同一位置改为 `Stall.lockedHint = {title:'？？？茶区 · 尚未解锁', desc:'听说那边有一种很特别的茶……'}`。RNG 消耗路径不变，确定性保留。lockedHint 是纯展示位：无价格/品质/购买按钮/茶叶数据，不进背包。
- **MarketView.tsx**：`generateStalls(player.day, player.currentRegion, player.flags)`；摊位商品列表顶部渲染 lockedHint（`.stall-tea--locked` 虚线降饱和样式，base.css）。实际只会出现在武夷山集市老贾摊（hangzhou 摊主的对方=武夷山恒解锁）。
- **偶遇层审计通过**：老贾事件只给武夷山茶（rougui/shuixian/dahongpao/wangba，wangba 另锁 currentRegion==='wuyishan'）；乌牛早/九曲红梅仅牛姐（regions:['hangzhou']）。偶遇层无杭州茶泄漏。
- **顺手修真 bug**：杭州摊主池 `'gushu'` 应为 `'gu_shu'`（npcs.ts 实际 id）——`getNpc('gushu')` 抛错，杭州集市抽到郭叔摊位必崩；已改 marketStalls.ts 四处（池+OWNER_PROFILE+WARE_PROFILE+SELLER_STYLE）。
- **esbuild 模拟验证（30 天×两茶区）**：杭州未解锁→杭州茶 0 款、彩蛋 0、预告位 13 次；杭州已解锁→杭州茶 8 vs 本地 239（少量流通）、预告位 0；杭州集市→武夷山茶 11 vs 本地 169、预告位 0。全部符合定稿规则。
- 改动文件：teaRegions.ts、TeaWorldView.tsx、marketStalls.ts、MarketView.tsx、base.css。基线 `92a41fa`，未 commit/push（含前序 items 1-11）。

## 杭州偶遇池扩充（新 5 NPC 复用 + 杭州语境重写，已实现，未 commit/push）
- 用户定稿：请喝茶大姐/年轻茶农/卖茶大叔/路边饮茶叔/神秘茶人入杭州随机偶遇池；复用身份美术不新增；神秘茶人低概率机制不变；普通偶遇 NPC 在杭州只涉及杭州本地茶；老贾/牛姐定位不动；对白要杭州语境（不能搬武夷山版），「我的回应」同步本地化。
- **关键事实**：杭州偶遇仅发生在共用 `market` 场景（EncounterLayer 只挂 wuyishan 的 teahouse/garden/mountain + 共用 market；hz-* 场景不接偶遇层）→ 只需适配 5 NPC 的 market 事件；garden/mountain/teahouse 事件在杭州永不触发（注释已说明未来接层前需补 linesByRegion）。
- **encounters.ts 改法**（不改引擎/类型）：
  - 5 NPC `regions: ['wuyishan'] → ['wuyishan','hangzhou']`；文件头注释补杭州池构成与规则。
  - 聊天事件用 `linesByRegion.hangzhou` 覆盖台词（含「你」回应：梅家坞/清明前/一芽一叶/豆香/玻璃杯/九曲红梅等杭州元素）；`dajie_market`、`youngfarmer_market`、`dashu_chat`、`uncle_market`、`uncle_chat`、`mystery_appear` 已覆盖。
  - 赠/售茶事件按茶区一分为二（requires 锁 currentRegion，同 laojia_wangba 模式，权重不变）：`dajie_offer_hz`(九曲红梅·普通 unitValue16)、`dashu_fair_hz`(卖龙井·普通 -8文)、`dashu_pit_hz`(礼盒九曲红梅 -8文)、`mystery_gift_hz`(龙井·上品 unitValue36)；杭州茶 roastLevel 用「到位/刚好」（非足火）。
  - `_nothing` 中性旁白共享；神秘茶人 scenes 权重 mountain4/market2 完全不动。
- **模拟验证（esbuild，40k 次 Monte Carlo）**：
  - 杭州 market NPC 份额：老贾 25.2% / 卖茶大叔 17.5% / 年轻茶农 10.5% / 路边饮茶叔 9.7% / 大姐·林姑娘·旅客 9.1-9.4% / 牛姐 8.4% / 神秘茶人 0.8%（=武夷山 0.8%，未提率）。
  - 杭州实际可触发事件除老贾（设计=已解锁外地茶流通）外全部为杭州本地茶；王霸茶仍唯一出自 laojia_wangba（锁武夷山）；乌牛早仅牛姐。
  - 「我的回应」动画：EncounterLayer 对所有 region 统一按 speaker 分气泡（你=右绿），linesByRegion 台词自动走同一动画，无需改组件。
- 验证：`tsc --noEmit` + `build:web` + `build:xhs` 全绿；零 IS_XHS 分支；SHARE_PAYOUT_ENABLED 仍 false。
- 改动文件：encounters.ts（唯一）。基线 `92a41fa`，未 commit/push（含前序 items 1-11 + 跨区流通规则）。

## 小满王霸茶剧情重复触发 bug 修复（已实现，未 commit/push）
- **用户报告**：买王霸茶后每次和小满对话都重复触发怀疑剧情。**根因**：`NpcDialog.buildSteps` 在 first/conditional/repeat 都没命中时兜底 `chain = pool`（整池重播）；小满 market 池只有 first + wangba 条件对话、**没有 repeat 对话** → 剧情完成（wangba_xiaoman_done 落盘、条件过期）后，每次进集市整池兜底重播「初次见面 + 已过期的王霸剧情」。这是池缺 repeat 的结构性坑，不是 flag 没落盘。
- **修复（最小改动）**：`dialogues.ts` 新增 `xiaoman_market_repeat`（trigger kind repeat，两句日常招呼）→ 见过面后 ordered 恒非空，永不走整池兜底。注释写明该坑。
- **核对既有机制本就符合用户要求**：触发绑定 `bought_wangba`（只在老贾偶遇购买瞬间由 3 处 setsFlags 置位，**不看背包/库存**）；一次性=`wangba_xiaoman_done` 经 setFlags→persist 落盘；仅武夷山（派生条件锁 currentRegion）；MarketView 的「偶遇结束→setTalked(false) 重挂小满」实现购买后当场反应。
- **同类隐患排查**：老陈(teahouse 含 repeat)/岩伯(母树 repeat)/玲姨(hz-teahouse 含 repeat)均有 repeat→安全；`ZhouBoTableDialog` 自带 `first ?? cond ?? 话题` 选择、无整池兜底→周伯辨茶安全。
- **esbuild 模拟（复刻 buildSteps 全逻辑）**：6 状态全过——初见→first；见过未买→repeat；刚买→suspect 单独播；done 落盘→repeat；背包有茶但非购买 flag→repeat；杭州→repeat。
- 验证：`tsc --noEmit` + `build:web` + `build:xhs` 全绿。改动文件：dialogues.ts（唯一）。基线 `92a41fa`，未 commit/push。

## 岩茶火功丰富化（三茶做青/焙火差异 + 火性软分，已实现，等用户确认后提交）
- 用户定稿方向：做青与焙火分开塑造（肉桂=轻做青·中火求香；水仙=从容走水·中足火求醇；大红袍=平衡·火功灵活）；火性 20 分**只做软加成无硬门槛**；大红袍轻/中/足均为有效风格；高火=风格标签（焦糖香），病火=焦味/炭化（负面），两者分开评价；数字只作校准目标。
- **实现**（评分构成：命中质量×60 + 稳定性×20 + 火性合拍×20 − 病火惩罚，casual+5）：
  - 新建 `core/making/roasting.ts`：纯函数 `evaluateRoasting`（组件与模拟验证共用同一份逻辑）；`STYLE_CURVE` 三条 lean→fit 连续插值曲线（肉桂 ideal=0、水仙平台 0~0.045、大红袍宽平台 ±0.05）；`ROAST_REVIEW` 3 茶×6 档火功点评（欠火/轻火/中火/足火/高火/病火）。
  - `scoring.ts`：`computeRoastLevel(score, haste, lean)` 六档判定；导出 ROAST_LEAN_T=0.03 / ROAST_LEAN_T2=0.075 / ROAST_OVER_HASTE=2.2。
  - `teas.ts`：gameProfile 加 `roastStyle`（aroma/mellow/balanced）；roastBias 调整（肉桂 .04/-.02、水仙 -.04/+.03、大红袍 0/-.03 最窄）；TEA_STEP_OVERRIDES：肉桂 zuoqing [0.42,0.66]（轻做青）、水仙 zuoqing idleLimit:14（从容走水）。
  - `types.ts`：TeaGameProfile.roastStyle / StepParams.idleLimit / StepOutcome.lean（全为加法）。
  - `RoastingStep.tsx`：finish 走 evaluateRoasting；每轮 note 分方向（偏左「火还没跟上来」/偏右「火有点急了」/正中按稳处理）；开场按 roastStyle 给火性心法。
  - `ZuoqingStep.tsx`：idle 上限参数化（10→params.idleLimit）；加 teaId prop + 茶种做青心法（TEA_ZUOQING_HINT）；MakingFlow 传 teaId。
- **蒙特卡洛校准**（N=20000/格；其它工序同分布隔离变量；同噪声对旧公式基线复刻对比）：
  - ① 三茶上品率均下降：熟手(典型玩家) 肉桂 12.7→9.6%、水仙 14.5→11.1%、大红袍 12.6→11.6%；生手降更多；老手懂茶性者基本持平（学习回报）。
  - ② 良好率基本稳定（掉落的上品进入良好）。
  - ③ 大红袍三档火功上品率：轻火 5.5% / 中火 15.1% / 足火 6.8%——均可行，中火（平衡）最容易。
  - 策略区分度：肉桂「习惯偏足火」老手 6.9→2.9%（重罚）；水仙同习惯 11.5→10.1%（耐焙不罚）✓。
  - ⚠️ 教训：模拟第一版 gauss 噪声 std 写错（×0.29）导致「人人中火」假象；修正后 dist/lean 分布才真实。火性分从档位阶跃改连续曲线是关键校准（阶跃时瞄中心=满分、上品率不降）。
  - 高火/病火在正常游玩中罕见（haste 累积机制保守，属灾难场景），与旧版一致。
- 验证：`tsc --noEmit` + `build:web` + `build:xhs` 全绿；零 IS_XHS 分支；SHARE_PAYOUT_ENABLED 仍 false。
- 改动文件：roasting.ts(新)、scoring.ts、teas.ts、types.ts、RoastingStep.tsx、ZuoqingStep.tsx、MakingFlow.tsx。实现前备份 `/tmp/tea-game-backup-20260913-roastwork.tar.gz`（426K，src+docs）。基线 `92a41fa`，**等用户确认后再提交**。

## 九曲红梅制茶盘点 + 发酵方案 B 设计（只分析未改码）
- 盘点结论：四步骨架合理不需重做；萎凋是唯一「数字直显答案」的步骤（meter+区间 50–57）；揉捻「轻→重→轻」未建模；发酵问题定性=反馈被直译（aromaRough 常显直说答案、色卡自动高亮）+ 翻堆无目的（f−4 不计分无均匀性支承）。
- 用户选方案 B：只加一个隐藏「堆温」状态（不显示数字），翻堆=散热+发酵加速的解药；香气改主动观察模糊分档；色卡去高亮；结算文案四态（偏早/合适/偏晚/过度）。设计详见当轮对话；实现预计仅改 FermentationStep.tsx 单文件。
- 发酵方案 B 定稿前分析：①参数校验通过——不翻堆时 f 到窗口中心 ≈7.1s、闷线(heat70)出现在 f≈54（黄褐将现），加速上限 ×1.36 不会失控；②turn(heat−32/f−3) 校验——一次翻堆完全退出加速区，二次热信号恰在出堆决策点形成取舍；③定稿 **不保留 heat>70 的 −3 软扣**（惩罚「翻晚但出堆正确」的玩家，违背「翻堆非必点」原则），只保留结算文案「香里微微带闷」；④泄露清查：色卡高亮、常显 aromaRough、leafDetail 指令尾巴三处全删，堆况顶层文案只承担「该翻」信号不承担「该出堆」信号；⑤复杂度对比：发酵 f+heat 两量仍显著低于做青（三轴+天气+受伤）与焙火（lean+曲线），单文件可实施。
- 发酵方案 B 已实现（仅 FermentationStep.tsx：heat 状态/tick 加速/turn=heat−32+f−3/stackStatus+aromaTier+leafDetail 文案/色卡五档去高亮/HeatMist SVG/结算四态+堆温叙事，未加 −3 软扣）。tsc+build:web+build:xhs 全绿。
- 三组手感模拟：never（闷线 6.1s/f61，后段烫手压力强）；once（闷线推迟到 8.1s/f78，最佳出堆点 7.1s 时堆仅「温热」→ 翻堆=可选舒适优化）；twice（闷线 10.1s，犹豫兜底）。三组均自然走到 f=100 自动结束=「不是越久越好」由机制自证。
- 出堆命中率：双信号（色卡红润+甜润香）64.4% 窗口内、0% 出界 fault；只闻香 3.7%（甜润档太宽，必须结合色卡=综合判断的设计意图，偏早 50% 会被结算文案解释）；新手乱出 10%（随机基线）。
- Q1/Q2（会不会主动点闻/看、会不会想到翻堆）需真人试玩：验收 checklist 已交用户（首次进入观察按钮点击率、闷线出现后翻堆率、出堆后能否复述原因）。
- 九曲红梅采茶审计：picking 配置确为岩茶模板（open-face + middle-open）；但 PickingStep 是数据驱动组件（open-face/bud 两模式），TeaShootSvg 已有 bud1/bud2 嫩芽形态，杭州侧已有阿青+HangzhouGardenScene 注入 → 改造成本极低：teas.ts 一行配置 + MakingFlow 红茶版教程常量 + picking 教程 flag 按模式拆分（现有 picking_taught 全局 gate 会跳过红茶版教程，唯一跨茶种状态注意点）。
- 发酵视觉与布局审计：色卡行隔开叶堆与堆况行 → 最小改法=堆况条移入叶堆容器底部（absolute，零 CSS 文件）；变红过快的根因=fermentColor 分段阈值（25/50/75）相对窗口（70-76）过早，方案=fV=(f/100)^1.35×100 纯视觉重映射，机制不动。
- 九曲红梅专属体验已实施：①采茶切 bud 模式（teas.ts 一行 + targetMaturity:'bud-one-leaf'，复用 PickingStep/SVG 零组件改动）；②MakingFlow 新增 HZ_HONGCHA_TUTORIAL/BASKET（阿青红茶语境，「一芽一叶~一芽二叶初展都正当时」，删 HZ_PICK_* 死代码）；③教学 flag 按模式拆分（bud 用 picking_taught_bud 独立 flag、不回退 picking_taught——否则老档会跳过 bud 教学；开面仍用原 flag）；④发酵堆况条移入叶堆容器底部（absolute 状态条，叶+堆况=视觉单元）、色卡下移到出堆按钮之后作纯参照；⑤颜色视觉滞后 fV=(f/100)^1.35×100（仅 fermentColor/背景/redEdge 三处输入，f/heat/评分不动）——验证：青绿 0-3.5s/黄绿 3.5-5.5s/金黄 5.5-7s/转红 6.9-7.6s≈窗口(7.1-8.0s)/偏暗 8s+，红润与窗口同步。
- tsc+build:web+build:xhs 全绿。改动文件：teas.ts、MakingFlow.tsx、FermentationStep.tsx。未 commit/push。
- 采茶小字串区修复（截图 bug）：泄露源=STEP_META.picking.simplificationNote（「岩茶开面采…」对所有茶显示）+ knowledgeComicId='comic_wuyishan_terroir' 在杭州采茶也解锁武夷山风土漫画。修复：①PickingConfig 加可选 knowledgeNote（types.ts 一行）；②龙井/九曲红梅 gameProfile.picking 各配嫩采知识小字（武夷山三茶缺省回退原岩茶 note，原文不动）；③MakingFlow 底部小字 picking 时按茶种取（stepNote）；④知识漫画解锁加 regionId 守卫（comic.regionId===tea.regionId，全表现有映射均兼容）。杭州采茶可见文案复查：「开面」仅剩龙井教学对照句与武夷山专用教程，无岩茶知识串区。tsc+双端构建全绿。未 commit/push。
- 茶集市两项体验已实现：①卖茶结果页加「回到茶集市」按钮（setResults(null)+setMode('home')，复用小满入口状态，零新导航）；②熟客回访轻量剧情（失败茶抱怨/上品茶好评，30% 概率进集市判定）：Player.teaFeedback 单条待回访 {teaId,kind,fault}；TeaStack 加 fault（fail 栈由 finishMaking 传入 result.worstFault，scoring 新导出 worstFault）；sell 时失败/上品覆盖记录；新文件 core/data/marketFeedback.ts（FAIL_LINES 按 茶种×FaultTag 精确匹配+default、FINE_LINES 五茶安利文案）；MarketView 挂载时 30% 判定→熟客面板（复用 dialog-bubble 样式）→继续时 consumeTeaFeedback() 清除。无声望/口碑/惩罚数值；经济零改动。tsc+双端构建全绿，未 commit/push。
