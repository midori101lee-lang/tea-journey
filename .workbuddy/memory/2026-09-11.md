# 2026-09-11 工作日志

## 诊断：用户反馈「茶园仍有景区王霸茶」「天气系统没感觉到」——根因是未部署，不是代码 bug

### 核心结论
- `origin/main` 与本地 `HEAD` 都停在 `4dc2b87`（feat: 茶世界入口 + 入口顺序修正 + 泡茶消耗 + 王霸茶调参 + 茶具资源优化）。
- 之后的全部改动**一直是未提交状态**（11 个 M + 3 个 ??）：周伯引导重构、天气系统 V0.1、品饮茶具裁切修复、王霸茶误入茶园修复。
- 所以线上 `tea-journey.pages.dev` 跑的仍是旧包 → 两个现象都由此解释（旧 TeaSelect 用 TEAS 全量、无天气模块）。
- 用户此前说「我直接 push 吧 / 我准备 push 一下」，但因 Bash 工具报错 + 未实际执行，push 从未发生。

### 本地代码已核实正确且全绿
- `TeaSelect.tsx:12` = `YANCHA_RECIPE.appliesTo.map(getTea)`；`teas.ts:165` `appliesTo:['rougui','shuixian','dahongpao']`（不含 wangba）✅
- 天气接线齐全：MapView→WeatherBadge；NpcStage→WeatherOverlay；WebApp 66/83/134→NpcWeatherAside(老陈/阿秀/周伯)；MakingFlow 31-33 算天气→DaoqingStep(zuoqingRate)/ZuoqingStep(weather) ✅
- 本地 dev server(5173) curl 实测：TeaSelect 模块含 `appliesTo`、MapView 含 `WeatherBadge`、NpcStage 含 `WeatherOverlay` → dev 已在跑修复后代码。
- `npm run typecheck` ✅；`npm run build:web` ✅（web-26Xs4fmv.js 337.81 kB）。

### 确定性天气序列（wuyishan）
Day1 🌧️小雨 / Day2 ☀️晴 / Day3 ☁️多云 / Day4 🌧️小雨 / Day5 🌫️山雾 / Day6 🌫️山雾
→ 新档 Day1 就是小雨，首页天气卡应显示「🌧️ 小雨 · 山里落着细雨…」+ 场景雨丝 overlay。

### 待办
- 用户 push 该批次到 `origin/main`（SSH）→ Cloudflare 重建后，上面两现象应消失。
- 若用户在 localhost 验证：硬刷新 5173 即应看到茶园无王霸茶、武夷山首页有天气卡。

## ⚠️ 关键发现：tea-journey.pages.dev 不是我们的站点（线上验证 URL 一直错）
- 用户一直用 `https://tea-journey.pages.dev` 验证线上效果。但 curl 该域名返回的 HTML 是**别人的作品集页**（《Tea Journey - Project Detail》，作者 ZiWang0531，链接指向 zi-wang.pages.dev / github.com/ZiWang0531/portfolio）。
- 该 `pages.dev` 子域被他人占用，并非我们的 Cloudflare Pages 站点。所以此前"线上王霸茶还在 / 天气没生效"的观察，看的是别人家的静态页，自然不会因我们的 push 改变。
- 我们的 GitHub push 已成功（e70d1f0 → origin/main），Cloudflare 应从正确源码重新构建。但正确的部署 URL 不在仓库里（README 只泛写"Cloudflare Pages"），需用户从 Cloudflare 控制台查其真实 `*.pages.dev`（项目名未必是 `tea-journey`，因该子域被占）。
- **行动**：让用户到 Cloudflare Pages 控制台确认绑定 `midori101lee-lang/tea-journey` 仓库的项目的真实地址并贴出，再curl 该 JS 资产核对 WeatherBadge/appliesTo 是否上线。
- **待办**：拿到正确 URL 后，修正本仓库 MEMORY.md 部署约定里的 `https://tea-journey.pages.dev`（当前是错的）。
- 本地验证路径始终有效：硬刷新 localhost:5173 即可看修复后效果。

## ✅ 正确 URL 与部署核验（同上轮）
- 用户给出正确线上地址：**`https://tea-journey-d2z.pages.dev/`**（已更新进 MEMORY.md 部署约定）。
- curl 线上 `assets/web-26Xs4fmv.js`（size 364486，与本地 dist 09:10 构建**哈希/体积完全一致**）→ 同一份产物，Cloudflare 确实从 e70d1f0 重构建成功。
- **天气实际已上线**（更正上轮误判）：包内含天气文案 `山里落着细雨`/`山雾`/`今天天气不错`/`weather-mist`/`weather-rain` 各 1 处；茶园修复 `appliesTo` 2 处。
- 上轮 `WeatherBadge/WeatherOverlay/NpcWeatherAside/getWeatherForDay` 显示 0 是**误报**：Vite 生产构建把这些组件名压缩重命名了（本地 dist 同查询也是 0），不能作为"没部署"的证据。
- 用户"没感觉到天气"的真实原因：① 此前一直看的是错的 `tea-journey.pages.dev`（别人家站点）；② 正确地址上线后天气本身克制——Day1=🌧️小雨（仅淡淡雨丝），且天气卡只在**武夷山首页**顶部明显。
- 结论：push 与部署均成功，无需重推。后续验证请用 `tea-journey-d2z.pages.dev`，进武夷山首页看天气卡。

## 核查：两个体验问题现状（用户问"解决没有"）
- **移动端泡茶品饮显示**：已解决且已上线。`765a1eb「优化三茶泡茶视觉与移动端交互」`（e70d1f0 的祖先）把 `.brew-stage` 由 320×250 放大到 380×320 并重定位盖碗目标点；`e70d1f0「品饮茶具裁切」`修了品饮阶段茶具图裁切。工作区还有 `.brew-gaiwantarget` 改 CSS 居中（未提交）。残留：GAICENTER={x:190} 仍按 380px 硬编码，<380px 窄屏 ≤10px 偏移，但 NEAR 容差 70px 够用，不影响判定。
- **茶具参与感不足**：已大幅改善。线上初版已含「选茶具步骤 SelectWarePhase」+「出汤公道杯(SVG)」。工作区未提交增强：`tray` 新类型(竹木茶具 set→tray，泡茶底层 TrayLayer)、投茶阶段茶叶罐角落"开罐"互动、公道杯换 webp+流入动画、所选盖碗/壶全程出现；全部纯视觉不进评分。typecheck 通过。
- ⚠️ 工作区未提交改动（与 V0.4 制茶无关，属泡茶/茶具）：`BrewingFlow.tsx`、`base.css`、`teaWares.ts`（M）；临时脚本 `_grade_sim.mjs`/`_verify_tea.mts`（未跟踪）。`git status` 把 `竹木茶盘.webp` 显示删+新增是 macOS Unicode 规范化假象，磁盘/构建正常。

## 茶叶罐交互重做（投茶阶段，未提交）
- 痛点：茶叶一开始就悬浮显示、茶叶罐缩右上角 46px 小图、开关不真正改变状态、无取茶选择、视觉无"从罐取茶"关系。
- 改动（仅 AddPhase + base.css，不碰制茶/评价/消耗/公道杯/茶盘/茶市/NPC/杭州篇）：
  - 拥有茶叶罐时，投茶阶段底部出现轻量选择 `怎么取茶？[使用茶叶罐][直接投茶]`，默认选中已购茶叶罐但不强制；无罐则无此选项、直接默认投茶。
  - 「使用茶叶罐」模式：茶叶罐进入主体操作区左下（104px 点击区 / 84px 图，原 46px 右上角），初始**关闭**；茶叶在打开前不可见。点击罐子 → 罐盖 SVG 位移掀起 + 罐口变深 + "点我打开"提示消失 → 茶叶才从罐口附近出现 → 拖入盖碗 → 完成。
  - 复用现有 `ownedWareOfType(owned,'caddy')` 判断，未新建库存。开合用纯 SVG 罐盖 + CSS transform，无新图片。
  - 无茶叶罐 / 选"直接投茶" → 走原默认投茶，流程不变；泡茶仍为 7 步。
- 验证：typecheck 通过；`npm run build:web` 通过（101 模块）。移动端：罐足够大、点击区>图、不遮盖碗(计算无重叠)、不遮文字(选择在 foot)。交互靠代码审查+构建验证，未做真机拖拽。

## V0.4 制茶系统体验优化（未提交，按 spec 不 commit/push）
用户给明确边界：先读代码→保留核心玩法→只优化反馈/权重/稳定性→实测，不自由发挥重做。5 项优化：
1. **倒青权重 10%→15%**（scoring.ts `WEIGHTS.daoqing: 0.15`，仍低于做青 0.30）；DaoqingStep 由笼统 `daoqing_off` 拆为三类归因：`daoqing_short`(轮次不足)/`daoqing_overlong`(晾太久>6)/`daoqing_off`(软度/绿度误差>0.5)；FAULT_PENALTY/FAULT_REASON 各加两条（罚 8）。
2. **FAULT_REASON 真正入结果页**：scoring 不再用 fault 覆盖 comment，改为生成 `highlight`（有 fault=影响最大条；无=正向 PRAISE 按步）。ResultView 重构为三段「品质等级 / 这一锅怎么样(comment) / 这次最值得注意(highlight，fault 时--cliff 红、否则--bamboo 绿)」。
3. **焙火随机改确定性**：新建 `src/core/making/roastBand.ts`，`hash01`(FNV) + `computeRoastBand`，seed=hash01(teaId长度,day,round(prof),tapIndex)；rand=(seed-0.5)*randomDrift*0.6，最大约 ±0.02。**不新建随机系统**，移除 RoastingStep 内 `Math.random`；RoastingStep 加 `day` prop，由 MakingFlow 传 `player.day`。实测 2000 次同输入完全一致、跨 day 确定性漂移。
4. **三茶轻微手感差异**：teas.ts 加 `TEA_STEP_OVERRIDES`(rougui 倒青软度[57,76]/做青力度[0.47,0.70]/炒揉[0.42,0.68] 偏敏感；shuixian [52,80]/[0.42,0.75]/[0.38,0.72] 偏宽容；dahongpao 不覆盖=平衡) + `getStepParams(teaId,step,difficulty,bq?)` 合并 基础+覆盖+casual+采茶成色。不动 `gameProfile.difficulty` 死字段。
5. **未提高上品门槛**：Grade 阈值保持 fail<40/normal<62/good<80/fine。

### 实测（verify_v04.mjs，esbuild 打包真实 core 模块，非复刻）
A fine / B good(倒青归因) / C normal(做青归因) / D fine / E normal / F normal(焙火归因) 全符合预期；焙火 2000 次重复一致。临时脚本已删。
- `npm run typecheck` ✅；`npm run build:web` ✅（102 模块）。
- 修改文件 8 个：types.ts、scoring.ts、roastBand.ts(新)、teas.ts、MakingFlow.tsx、RoastingStep.tsx、DaoqingStep.tsx、ResultView.tsx。
- 未影响：采茶/泡茶/茶具/周伯/九龙窠/杭州篇/localStorage/天气；Core 与 UI 仍解耦（roastBand 为纯函数，无 React/DOM）。
- ⚠️ 工作区另有**茶叶罐/茶具**未提交改动（BrewingFlow/base.css/teaWares/teaware webp），与本次相互独立，需用户决定何时一并 push。

## 投茶阶段布局重构（AddPhase，未提交）
用户实测发现：切换「直接投茶/使用茶叶罐」时茶叶残留、茶叶罐偏低、罐与品茗杯不在同一水平线、像两套场景。核心整改（不碰泡茶业务逻辑）：
- 建立**固定茶具舞台**锚点：`base.css` 茶叶罐与新增 `.brew-cup`(白瓷品茗杯) 同置于 `top:196px` 底排，左/右对称、同尺寸(96)、互不遮挡、不挡居中盖碗。
- `BrewingFlow.tsx` 新增 `DIRECT_LEAF{x:132,y:236}`(直接投茶默认取茶点，盖碗下方居中) 与 `JAR_LEAF{x:10,y:120}`(用罐且打开后罐口上方) 两个锚点常量。
- `AddPhase` 重构：取茶方式默认「直接投茶」；`showLeaf = !inBowl && (mode==='direct' || (mode==='caddy'&&caddyOpen))` → **两模式互斥、不共用坐标、非显示态整节点不挂载**，从根上消除"切换后残留茶叶"。`chooseDirect/chooseCaddy/toggleCaddy` 在切换来源时把拖拽 pos 重置到对应锚点。
- 白瓷品茗杯(`white-teacup`)在投茶阶段固定展示于底排右（仅展示，不交互），与茶叶罐同水平线；茶叶罐仅「使用茶叶罐」模式出现。
- 投茶阶段盖碗 `size=130`（仅此处略缩以容纳底排茶具，落茶判定仍用固定 GAICENTER，不受影响）。
- 验证：`npm run typecheck` ✅、`npm run build:web` ✅（102 模块）。布局坐标推算：≥360px 宽不重叠；≤358px 极端窄屏侧具仅轻触盖碗边缘（中央操作区始终不被挡）。未改消耗/判定/Grade/茶汤/其他泡茶步骤。

## 泡茶「按阶段切换茶具舞台」重构（BrewingFlow 全面，未提交）
用户实测：茶具越买越多→泡茶画面拥挤、按钮被遮、茶具互压。根因：① `TrayLayer`(茶盘)此前在**每个阶段都当全屏背景渲染**；② 白瓷品茗杯此前误在投茶阶段出现；③ `.brew-foot`(操作按钮)原本嵌在 `.brew-stage` 内，绝对定位茶具可盖住它。本轮（**不新增茶具、不改购买系统、不改任何评分/判定/消耗/Grade/茶汤/茶市/杭州篇**）重做：
- **阶段化茶具可见性**：新增 `PHASE_WARES` 映射驱动「拥有≠每阶段都显示」。茶盘仅品饮、公道杯仅出汤+品饮、品茗杯仅品饮、茶叶罐仅投茶、盖碗/壶贯穿。各 Phase 只渲染自己所需→画面始终干净。
- **删除 `TrayLayer`**，茶盘改为品饮阶段专属 `.brew-taste-tray`（仅作承托底座，非全屏背景）；投茶阶段移除误加的 `.brew-cup`（品茗杯只在品饮出现）。
- **ActionBar 与 TeaStage 分离**：每个 Phase 改为 `<div className="brew-stage">…茶具…</div>` + 独立的 `<div className="brew-actionbar">…tip+按钮+选择…</div>` 兄弟节点；`.brew-stage` 有 `overflow:hidden` 且按钮移出舞台，茶具永远无法覆盖按钮。
- **品饮阶段重做成「小茶席」**：`TastePhase` 重构——茶盘(若有)作底层底座；主品饮器=公道杯(若有)否则冲泡盖碗(默认品饮器)居中坐在盘内；品茗杯(若有)居侧、同盛此泡、完整落在盘内；盖碗不再作主体。CSS 新增 `.brew-stage-taste`/`.brew-taste-tray`/`.brew-taste-primary`/`.brew-taste-cup`。
- 投茶阶段茶叶罐逻辑保留上一轮互斥锚点（DIRECT_LEAF/JAR_LEAF），投茶完成随阶段切换自动退出。
- 验证：`npm run typecheck` ✅、`npm run build:web` ✅（102 模块）。移动端坐标推算：≥360px 各阶段无重叠、按钮独立不被遮；桌面端结构不变。
- 修改文件：`BrewingFlow.tsx`、`base.css`（其余茶叶罐/茶具未提交改动同前，仍独立）。

## 存档系统（自动保存 + 手动保存 + 继续旅程，未提交）
用户要求增加可靠存档：避免刷新/关页丢进度。先读架构发现**已有 `teaworld.save.v3` 持久化**（storage.ts 的 persist 在每次 player 变更自动写盘；gameStore.ts 初始化 `player: loadSave().player` 且 loadSave 只读取从不覆盖→天然防止初始化覆盖旧档）。
本轮在既有架构上补缺失件（不新建第二套 state、不改任何茶叶/评分/Grade 业务）：
- `storage.ts` 增强：`KEY=teaworld.save.v3` + `BACKUP_KEY` + `ARCHIVE_KEY`；`loadSave` 损坏档隔离到 `.corrupt` 不白屏；新增 `hasSave()`、`inspectSave()`(none/ok/corrupt)、`archiveCurrentSave()`、`wipeActiveSave()`、`clearSave()`；`persist()` 写前把旧档复制为 backup；`migrate()` 旧 v1/v2 → v3 并回填 madeTeas/regionDays/proficiencyByRegion 等缺失字段（防旧档崩溃）。
- `gameStore.ts`：初始 `scene:'start'`（新增到 Scene 联合类型）；新增 `toast`/`showToast`(1.8s 自清)/`saveProgress`(再落盘+toast「进度已保存」)/`bootTo`；`reset()` 改为先 `archiveCurrentSave()`+`wipeActiveSave()` 再写全新档。所有长期状态(金币/背包/制茶/madeTeas/茶具/区域解锁/flags/saw_mother_tree/明信片/茶市/proficiency/day)经既有 persist 自动保存。
- 新增 `StartScreen.tsx`（boot gate：无档→开始茶旅；有档→继续旅程/新的茶旅(二次确认，不覆盖)）；`Toast.tsx`（根层全局轻量提示）；`main-web.tsx` 挂 `<Toast/>`。
- `TeaWorldView.tsx` 右上角 ⋯ 菜单：「保存进度」(saveProgress)+「重新开始/继续」(bootTo('start'))；CSS 新增 `.tw-menu*`/`.start-*`/`.toast`。
- `XhsApp.tsx` 把 `'start'` 并入 home CTA 分支（xhs 走线性精简流，不显示 web 启动页）。
- **防重复扣茶**：`brewingStackId` 为瞬时 UI 态不写盘→刷新后为 null，finishBrewing 不扣；茶叶仅在完整泡茶结算扣 1 包。
- 验证：`npm run typecheck` ✅；`npm run build:web` ✅（104 模块）。persistence 核心 esbuild 打包真实模块 + localStorage mock 实测 21/21 通过（新档/金币/茶具/saw_mother_tree/backup/旧 v1 迁移/损坏隔离/clearSave/archive 全绿）。
- 按约定未 commit/push。工作区此前茶叶罐/茶具/泡茶布局未提交改动 + 本次存档改动，均独立待用户决定推送。

## 杭州篇 Phase 1 · 基础玩法骨架（未提交）
用户 spec：让杭州作为第二个茶区真正进入现有游戏，跑通「茶世界→杭州→玲姨茶馆→杭州茶园(阿青)→九曲红梅→采茶→制茶坊→制茶→获得→茶桌→泡茶」最小闭环；梅家坞仅入口；龙井锁定；茶集市共用。**未做**龙井玩法/铁砂掌/明前雨前/茶点(绿豆糕)/茶席等。
- **区域架构**：`regions.ts` 加 `hangzhou` region（5 固定场景 + 梅家坞；market 共用）。`LocationDef` 加 `scene?` 绑定本地场景；新增 `regionLocationScene(regionId, locId)`。`LocationId` 加 `meijiawu`。`getRegion('hangzhou')` 必须存在（DayIntro/Journal 依赖）。
- **场景**：新增内联 SVG 占位 `HangzhouTeahouseScene/HangzhouGardenScene/HangzhouWorkshopScene/HangzhouTeatableScene/MeijiawuScene`，注册到 `SCENES`：`hz-teahouse/hz-garden/hz-workshop/hz-teatable/meijiawu`。正式背景图就绪后只给 key 加 `bg` 即可（不改玩法）。
- **NPC**：`npcs.ts` 加 `lingyi`(玲姨·茶馆中枢·recurring)、`aqing`(阿青·茶园小孩)。`npc picture/杭州特色npc/玲姨.png、阿青.png` 是透明 RGBA，已转 webp（1400 高）到 `public/assets/npcs/hangzhou/` 并接入 portrait（spec 第七条）。背景仍 SVG 占位（spec 第四/十五条）。
- **对话**：`dialogues.ts` 加 `lingyi_teahouse_first/repeat`、`aqing_garden_first/repeat`（scene = hz-teahouse / hz-garden）。
- **茶叶与配方**：`teas.ts` 加 `jiuquhongmei`(红茶,initial)、`longjing`(绿茶,flag:longjing_unlocked 锁定)；新增 `HONGCHA_RECIPE`（picking→withering→rolling→fermentation→drying）；`getRecipe(teaId?)`/`getRecipeFor`/`getStepParams` 按茶选配方；`makeableTeasForRegion`。`UnlockCondition` 加 `{type:'flag',flag,note}`；`Tea.category` 扩为 yancha/hongcha/green。
- **步骤组件**：新增 `WitheringStep/RollingStep/FermentationStep/DryingStep`；`PickingStep` 参数化 `presenterNpcId/presenterName/sceneNode/tutorial/basketComment/hint`（缺省=武夷山阿秀）；`MakingFlow` 完全由 `recipe.steps` 驱动。
- **scoring**：WEIGHTS/FAULT_PENALTY 加 4 步；新 FaultTag `wither_short/wither_over/ferment_short/ferment_over/drying_over`；COMMENTS.jiuquhongmei；`computeResult` 过程标签按 category（岩茶=火功 / 红茶=发酵 到位|不足|略过|中 / 其他=工艺）。
- **路由**：`gameStore` Scene 加 5 个；新增 `enterRegion(regionId,target)`（切 currentRegion + 同步 day/proficiency/mountainVisitsToday + 压栈）；`finishBrewing` 回本茶区茶桌。`MapView` 按 currentRegion 渲染（REGION_ORDER；山路仅武夷山）。`TeaWorldView` 走 enterRegion（杭州卡 opened=true 便于验证；正式门禁应为 heard_about_hangzhou）。`WebApp` 加 hz-* 分支。`TeaSelect` 加 regionId；`ResultView` 接 regionLocationScene + 过程标签按 category。
- **验证**：`typecheck` ✅；`build:web` ✅（113 模块）；`build:xhs` ✅（75 模块）。esbuild 打包真实 core 实测 **33/33 通过**。
- 按约定**未 commit / 未 push**。工作区另有：`cha dian/绿豆糕.png`（用户素材、未 ignore、属未来茶点，本阶段不做）、记忆文件未提交。

## 杭州篇 Phase 2 · 梅家坞 + 吟诗老人 + 正式背景接入（未提交）
spec：梅家坞入口 + 吟诗老人（吟诗/引龙井/赠诗笺），不重构、不动武夷山/茶集市/茶具经济；接入已有正式背景。
- **接入正式背景**（用户 2026-09-11 说「可以接入啦」）：`background picture/{玲姨的茶馆,杭州制茶坊,梅家坞}.PNG`（1600×2848 竖版）→ webp 宽 1100 q82 → `public/assets/scenes/{hangzhou_teahouse,hangzhou_workshop,meijiawu}.webp`；`SCENES` 对应 key 加 `bg`（hz-teahouse/hz-workshop/meijiawu）。**杭州茶园仍无正式图 → 保留内联 SVG 占位**。
- **吟诗老人**：`npcs.ts` 加 `yinshi_laoren`（梅家坞老人，sceneArt='meijiawu'）；`ported`=`assets/npcs/hangzhou/yinshi_laoren.webp`（← `npc picture/杭州特色npc/吟诗老人.png` 透明 RGBA 转 webp 高 1400）。`SCENES.meijiawu` 加 `figure`（老人立在场景中）。
- **对话**：`dialogues.ts` 加 `yinshi_laoren_meijiawu_first`（首次：吟诗→闲谈→引龙井→赠诗笺；`setsFlags:{met_yinshi_laoren,longjing_unlocked:1,received_meijiawu_poem:1}`；`givesSouvenir:'poem_meijiawu'`）+ `_repeat`（短句、不再赠）。诗句为用户指定**原文**（唐韬《访西湖梅家坞茶村》），未改写。
- **梅家坞诗笺**（旅途收藏品，与玲姨「茶联」区分；茶联目前**代码里尚无实现**，无冲突）：复用既有 `souvenirs` 系统，扩展 `SouvenirDef` 支持 `kind:'note'`（无照片、逐行诗句 + `attribution` + `motto` + `giverName`；`photo` 改可选，向后兼容明信片）。`SouvenirView` 增加 note 分支 + 头部按 regionId 显示茶区名。文案：描述「陶冶情操，有缘再见。」、短说明「吟诗老人留下的一张小诗笺。」。CSS 新增 `.poem-*`。
- **NpcDialog** 小增强：`line.speaker` 与 NPC 名不同时按原样显示该说话人且不挂职务（支持「你」等玩家台词）；既有对话（speaker==npc.name）显示不变。
- **龙井解锁**：吟诗老人首次对话置 `longjing_unlocked`（沿用既有 `unlockCondition:{type:'flag'}`）。**新增 `isCraftable(teaId)`**：无已实现配方的茶（龙井）不可进入制茶，避免误用岩茶配方；`TeaSelect` 显示「已认得这块山场，制茶的法子还在后头」，`startMaking` 加守卫。
- **偶遇残留修复**：`EncounterLayer` 的 `.encounter-layer` 是 `pointer-events:none`，带未结束偶遇离开 garden/teahouse/market/mountain 时不会清理 → 会隐藏后续场景（母树/杭州/梅家坞）的 NPC。`WebApp` 在 scene effect 里：非偶遇场景一律 `clearEncounter()`。
- **验证**：`typecheck` ✅；`build:web` ✅（113 模块）；`build:xhs` ✅（75 模块）；esbuild 实测 **31/31 通过**（诗笺字段/诗句原文/first 赠礼且 repeat 不重复/npc/region/解锁+不可做判定/明信片未受影响）。
- 按约定**未 commit / 未 push**。

## 章节衔接 · 武夷山→杭州解锁 + 武夷山茶礼（未提交）
spec：杭州不再因「第一次与老陈对话」解锁；须「武夷山探索 5/5 → 回老陈茶馆 → 老陈收束+赠茶礼 → 林姑娘杭州线索 → heard_about_hangzhou → 杭州解锁」。并新增一次性「武夷山首次离开赠礼」。
- **解锁链改动**：`WebApp` teahouse 分支 `lingguReady` 由 `tea_made` 改为 **`isWuyishanExplored(player) && !heard_about_hangzhou`**。`teaRegions` 杭州卡片 `unlocked:false` + 新增 `unlockFlag:'heard_about_hangzhou'`；`TeaWorldView` 改为 `unlockFlag ? !!flags[unlockFlag] : unlocked`（旧存档已 heard → 保持解锁）。
- **探索 5/5 读取**：`regions.ts` 新增 `isWuyishanExplored(player)` = 复用 `regionExploration`，`total>=5 && visited>=total`（market 开市后 total=5）。**未新增第二套探索系统、未用熟练度**。
- **派生对话条件（不落盘、天然兼容旧档）**：`dialogues.ts` 导出 `DERIVED_DIALOGUE_FLAGS`（`wuyishan_explored` / `wuyishan_farewell_ready` / `lingyi_wuyishan_tea_ready`）；`NpcDialog.buildSteps` 在普通 flag 之前优先用派生条件匹配 conditional 对话。
- **老陈收束 + 武夷山茶礼**：新对话 `laochen_teahouse_farewell`（trigger=`wuyishan_farewell_ready`；`setsFlags.farewell_gift_wuyishan=1`；`givesTea` 肉桂/水仙/大红袍 各 1 上品，`giftTag:'farewell_gift_wuyishan'`）。放在 `laochen_teahouse_pro` 之前以确保优先。`Dialogue.givesTea` 为新字段，`NpcDialog.applyEffects` 调 `addGiftTea` + toast「🍵 名称 ×N 已放入茶篓」。`WebApp` teahouse 复用 `.gift-banner` 展示「老陈送你的武夷山茶礼」。
- **茶礼存储**：`TeaStack.source` 扩 `'made'|'purchased'|'gift'`，新增 `giftTag?`。`pushStack` 在带 giftTag 时用 `${teaId}:${grade}:${giftTag}` 成**独立一栈**（不与自制/购买同品质茶合并，保留「我的旅途」身份）。store 新增 `addGiftTea(teaId,grade,count,giftTag)`。**未改 grade、未覆盖已有茶、未新建库存系统**。
- **不可出售**：`MarketView` 卖茶列表 `filter(s => s.source !== 'gift')`（仍可正常泡饮）；附一句提示。
- **玲姨旅途记忆彩蛋**：新对话 `lingyi_teahouse_wuyishan_tea`（派生条件 `lingyi_wuyishan_tea_ready` = 还带着老陈赠茶 && 未被点评过）；台词「嗯……武夷山的茶，果然脾气大。」。**非好感度/任务/强制**；茶喝掉则不触发、不影响主线。`TeaStackPicker` 给礼物茶加「· 旅途茶礼」标记。
- **旧存档兼容**：`isWuyishanExplored` / `hasWuyishanGiftTea` / 派生条件全部即时计算 → 已 heard 的旧档保持杭州解锁；新逻辑只影响「尚未领过茶礼」的档。`farewell_ready` 含 `!heard_about_hangzhou`，故已 heard 的旧档不会重播章节、也不会补发茶礼。
- **验证**：`typecheck` ✅；`build:web` ✅（113 模块）；`build:xhs` ✅（75 模块）；esbuild 打包真实 core+store 实测 **32/32**（Case A–F 对话选择、赠礼数据、杭州卡片 flag、addGiftTea 独立成栈不覆盖自制茶、玲姨彩蛋一次性）。
- 按约定**未 commit / 未 push**。



## 杭州第二轮（16:30-17:00）：茶联 + 发酵优化 + 西湖龙井

**P0 茶联**：此前代码里根本没有茶联（不是触发坏了）。新增 `couplet_hangzhou` 收藏（kind:'couplet'）+ `CoupletArt` 竖条占位 SVG（120×420、透明背景、双线边框、竖排联语）。`WebApp` hz-teahouse：玲姨对话结束且未持有时弹「赠联面板」（收下 → `saw_hangzhou_couplet` flag + addSouvenir + toast）；旧档下次进茶馆补一次。收藏查看页新增 couplet 分支。

**P1 发酵**：`FermentationStep` 重做——去数字进度与精确窗口提示；色卡（青绿/黄绿/黄褐/红褐）对照 + 香气粗略档；新增「凑近闻一闻 / 翻看茶叶」两个观察动作（信息更细，非必须）；翻堆保留。评分/故障（不足/略过）与确定性窗口漂移不变。

**P2 西湖龙井**：新增 `GREEN_TEA_RECIPE`（采茶→杀青→理条→干燥）与 `FixationStep`（锅温持续上升、看准区间快手翻炒，与焙火摆针不同）、`ShapingStep`（抓/压/推三式按序做对，动手后提示隐藏、每步限时）。`TeaShootSvg` 加 bud1/bud2；`PickingStep` 支持 bud 模式（一芽一叶 1.0 / 一芽二叶 0.62 / 老叶 0，分布 5/4/3）。scoring 加 fixation/shaping 权重(0.30/0.30)、4 个 FaultTag、龙井评语池、绿茶过程标签（杀青：刚好/不足/略过/中）与视觉（扁平=好形、鲜绿）。龙井仍由 `longjing_unlocked` flag 解锁（梅家坞），现在 `isCraftable=true` 可做了。周伯泡茶口吻与建议加 longjing 分支（清亮鲜爽/豆香）。

**验证**：typecheck ✅；build:web ✅（web-CqzQT5R4.js 410KB）；build:xhs ✅；esbuild 真实 core 实测 30/31（1 条失败为测试断言写窄，产品逻辑正确）。武夷山/九曲红梅回归 ✅。跨章节隔离审计：UI 无茶名硬编码回退。MEMORY.md 已从 20KB 整合到 4.5KB。

## 杭州第三轮（20:30-21:20）：手感修正 + bug 修复 + 茶桌背景

- **理条重做（横向节奏）**：`ShapingStep` 改为横向手法带（抓→压→排开）+ 当前手法节拍环（1400ms/casual 1700ms 收缩）；环收拢(≥0.52)=踩拍、0.24-0.52=勉强、过快=rushed、按错=broken(记碎不清进度)、超时自动过。三个手法按钮横向等宽，按钮随节拍微缩放。非音游：无连击/分数/长条。茶叶变形动画保留（颜色同步嫩黄绿）。
- **杀青 miss 修复**：`FixationStep` 改「一轮一定论」——命中/失手本轮即结束，750ms 反馈后进下一轮、锅温重置 0.05（指针必然重现）；lock 防连点；over≥2→焦边，0 good→青味。移除 clicksPerRound。
- **杭州采茶场景**：`HangzhouGardenScene` 重写——删除左前景高竹林（遮挡源），改 远山/白墙黛瓦/低矮茶垄/小路/矮竹篱笆/薄雾，前景干净。
- **龙井茶色统一（嫩黄绿）**：scoring 绿茶干茶 #a8ae58（普通 #94995c）、采茶 bud #b3bc6e、杀青/理条 dryColor、teaVisuals.longjing leafColor #a8ae58、汤色 #e6e9ac/#aab562。
- **茶具越权修复**：`SelectWarePhase` 移除白瓷盖碗「随身」特判，只列已拥有可泡茶具；一件没有 → `DEFAULT_BREW_WARE`（素盖碗，内联 GaiwanSvg 绘制，非商品）；修了 React hooks 条件调用问题。
- **郭叔**：`npc picture/杭州特色npc/郭叔.png` → `public/assets/npcs/hangzhou/gu_shu.webp`(698×1400)；NPC+hz-workshop 首次/重复对话；`MakingFlow` 每道工序前一句按 茶类×工序 的提点（GU_SHU_HINTS）。KNOWLEDGE_OWNER 加 两条；吟诗老人 knowledgeScope 收敛到茶诗/文化。
- **林姑娘杭州化**：hz-teahouse 增 `linggu_hangzhou_chat`（派生条件 linggu_hangzhou_ready=见过且没聊过，一次性）；encounters.ts 加 `linesByRegion` 机制（类型+EncounterLayer 按 currentRegion 取台词），linggu_pick/travel 各配杭州台词（杭州场景暂不接偶遇层，机制就绪）。
- **杭州茶桌背景**：`杭州茶桌.PNG` → `public/assets/scenes/hangzhou_teatable.webp`(1100×1958, 230KB)；SCENES['hz-teatable'].bg 换为新图；武夷山 teatable=teahouse_new 不动；泡完路由本就按 regionLocationScene 走 hz-teatable。
- **验证**：typecheck ✅；build:web ✅(web-BsrEms1w.js)；build:xhs ✅；esbuild 实测 27/27（先前 2 失败为测试断言笔误）。未 commit/push。

## 茶联卷轴化（21:40-21:55）
- 素材：`background picture/{茶联上,茶联下}.PNG`（120×420 RGBA）→ `public/assets/couplets/{couplet_upper,couplet_lower}.webp`（原尺寸 q92，不改字不合并）。
- 新组件 `components/CoupletScroll.tsx`：两根木轴（轴头+轴杆+系绳，CSS）+ 纸面 reveal（宽度 0↔全宽过渡，cubic-bezier 轻回弹 ~850ms）；受控 `open`；收起=两根小卷轴并排。收藏页复用同一组件直接展开。
- 赠礼流程（WebApp hz-teahouse）：玲姨台词「这是给你的，一副小茶联…」→ 卷起态 +【打开茶礼】→ 展开 → 950ms 后【收下，挂进茶游记】（既有一次性逻辑不变）；点联面可收回/再展开；离开茶馆自动复位。
- 数据：SouvenirDef 加可选 `photo2`（下联）；couplet_hangzhou 改用 photo+photo2，删占位 lines；SouvenirDef.kind 仍单件收藏。
- 删除 `CoupletArt.tsx`（被替换）。CSS：`.cs-*` 替换旧 `.couplet-gift-*`；高度 `clamp(230px,46vh,420px)` 保证 9:16 不出框。
- 验证：typecheck ✅；build:web/xhs ✅；souvenirs 数据实测 5/5。未 commit/push。

## 杭州茶园春茶叙事 + 新背景（21:50-22:00）
- 背景：`background picture/阿青的茶园.jpg` → `public/assets/scenes/hangzhou_garden.webp`（1100×1856, 241KB）；SCENES['hz-garden'].bg 接入（NpcStage 有 bg 即用真图，SVG 组件仅作无图回退）；杭州五个场景背景至此全部为正式图。
- 采茶步骤内小场景也换真图：PickingStep 加 `sceneBg` prop（img object-fit:cover，优先于 sceneNode SVG），MakingFlow 对杭州传 hangzhou_garden.webp；武夷山仍用 TeaGardenScene SVG。
- 春茶叙事：改写 `aqing_garden_first`（沿用既有 first 触发=metNpcs 机制，无新 flag/新系统）——玩家「咦？远处好像已经有人在采茶了。」→ 阿青讲春茶忙、「嫩」字、挑顶上嫩芽、挑得嫩不嫩做出来的茶差远了；不塞「一芽一叶」等标准（留给采茶引导 HZ_BUD_TUTORIAL）。repeat 不变（一句短对白）。背景茶农=纯环境元素，无 NPC/交互。
- 验证：typecheck ✅；build:web/xhs ✅；esbuild 实测 8/8。未 commit/push。

## 采茶小场景回退 SVG + 玲姨×阿青轻量互动（21:58-22:08）
- 回退：PickingStep 撤销 sceneBg/img（删除 prop 与 .pick-bg img CSS），恢复 sceneNode=HangzhouGardenScene SVG；SCENES['hz-garden'].bg（场景视图真图）保留不动。
- 玲姨×阿青「叫回家吃饭」（无任务系统，纯对话串联）：
  - `lingyi_teahouse_first` 尾部加 3 句：玲姨提阿青跑茶园、拜托捎话、玩家「好，我去帮你叫她。」
  - `aqing_garden_first` 重写：玩家喊吃饭 → 阿青「搭把手，把这批茶做完」（不点名茶种，因选茶在后）→ 玩家「我也能搭手吗」→「跟着我学，春茶耽搁不得」→「嫩」字教学保留。
  - `aqing_garden_repeat` 改「玲姨是不是又让你来抓我回去吃饭？」
  - 新增 `lingyi_teahouse_aqing_back`（派生条件 lingyi_aqing_back_ready=见过阿青且未回应过；落 flag lingyi_aqing_back_done）：回茶馆玲姨道谢 2 句，无奖励。
  - WebApp 茶馆 footer 改「去吧——记得替我喊阿青回来吃饭。」
- 验证：typecheck ✅；build:web/xhs ✅；esbuild 实测 14/14。未 commit/push。

## 两个查看层 Bug 修复（22:05-22:12）
- **Bug2 杭州周伯茶桌显示老陈茶馆新**：根因不是 SCENES bg（早已是 hangzhou_teatable.webp），而是 NpcDialog 用 `dlg.sceneArt ?? npc.sceneArt` 选 NpcStage 场景——`zhoubo_hz_teatable` 对话没写 sceneArt，回退到周伯 NPC 默认的武夷山 'teatable' → teahouse_new.webp。修复=对话显式 `sceneArt:'hz-teatable'`。同类隐患一并修：`linggu_hangzhou_chat` 显式 `sceneArt:'hz-teahouse'`（林姑娘 NPC 默认是武夷山 teahouse）。**约定：跨茶区复用 NPC 的对话必须显式写 sceneArt**。
- **Bug1 收藏页茶联无开合**：SouvenirView 之前写死 `open`（永远展开）。新增内部组件 `CoupletRemember`（useState 收起态，点卷轴/回车开合，aria-expanded + 操作提示文案），复用 CoupletScroll 动画；上/下联仍为两个独立 webp，一次性获得逻辑未动。
- 验证：typecheck ✅；build:web/xhs ✅。武夷山 teatable（teahouse_new）未动。未 commit/push。
