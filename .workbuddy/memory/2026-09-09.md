# 2026-09-09 工作日志

## 茶集市 V0.4「生活化茶市扩展」落地（P0 + P1）
用户给了一份超长规范，要求把长期蓝图里"已成熟、现在值得上线"的部分挑出来，做增量开发（不重做核心）。

### 只读审计（先做，按规范第一章清单）
确认现有基础已具备：多 NPC 摊位、一个 NPC 多种茶、统一茶篓、买茶→泡茶闭环（`TeaStackPicker`+`startBrewFromStack`）、王霸茶事件。缺口：无"看看/问问"面板、无 sellerStyle 话术差异、无捡漏/买贵、无 NPC 购买记忆、茶叶无来源信息。

### 改动文件
- `src/core/types.ts`：`TeaStack` 加 `sourceNpc`/`bargain`；`ProcessingResult` 加可选 `sourceNpc`/`bargain`（仅携带购买茶标记，供周伯反馈）。
- `src/store/gameStore.ts`：`pushStack`/`buyTea`/`addTea` 携带 `sourceNpc`/`bargain`；`startBrewFromStack` 把购买标记带入泡茶结果。
- `src/features/market/marketStalls.ts`：加 `SELLER_STYLE`、`TEA_LOOK`（看看茶）、`ASK_QUESTIONS`+`ASK_ANSWERS`（按性格应答）、`generateStalls` 计算 `bargain`（deal/overpriced）。
- `src/features/market/MarketView.tsx`：摊位内加 list/look/ask 子视图；每款茶【看看】【问问】【买下来】；熟客招呼（flag `bought_from_<npcId>`）；买下写入来源+标记。
- `src/features/brew/BrewingFlow.tsx`：周伯对 deal/overpriced 给"捡着了/下次再看看"反馈（无数值奖惩）。
- `src/features/brew/TeaStackPicker.tsx`：选茶显示"（XX的摊）"来源。
- `src/styles/base.css`：`.stall-tea-actions` / `.tea-detail` / `.ask-panel` / `.ask-q` 样式。

### 验证
`npm run typecheck` 通过；`build:web`（94 模块）、`build:xhs`（65 模块）均通过。

### 范围说明
本轮做了 P0（逛摊/看茶/问茶/统一茶篓/买→泡闭环/王霸茶保留）+ P1（捡漏/买贵/话术差异/来源/NPC 轻量记忆）。
P2（熟客完整事件/茶山线索/特殊商人/人情事件/Journal「我的发现」清单）尚未做，仅做了"熟客招呼变体"轻量版，留作下一轮。
